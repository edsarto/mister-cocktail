<h1>Add a dose to your cocktail </h1>

<h2><%= @cocktail.name %></h2>

<%= simple_form_for([@cocktail, @dose]) do |dose| %>
  <%= dose.error_notification %>

  <%= dose.association :ingredient %>
  <%= dose.input :description %>

  <%= dose.button :submit %>

<% end %>
